#ifndef FFT_DECRYPT_DECRYPT_WINDOW_H
#define FFT_DECRYPT_DECRYPT_WINDOW_H

#include <QMainWindow>

class DecryptWindow : public QMainWindow
{
public:
    explicit DecryptWindow(QWidget* parent = nullptr);
    ~DecryptWindow();
};

#endif
